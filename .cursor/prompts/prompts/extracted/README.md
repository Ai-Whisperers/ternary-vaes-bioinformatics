# Extracted prompts (optional)

This folder is reserved for prompts that are **extracted from real conversations** and then curated into reusable assets.

If you add extracted prompts here:

- Prefer converting them into proper Prompt Registry prompts (`*.prompt.md`) once they stabilize.
- Add them to an appropriate collection under `.cursor/prompts/collections/`.
